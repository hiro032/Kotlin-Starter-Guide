package chapter11

fun isDirectoryPath(path: String): Boolean {
    return path.endsWith("/")
}

class Car (
    internal val name: String,
    price: Int
) {
    var price = price
        private set
}

